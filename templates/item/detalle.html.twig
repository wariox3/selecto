{% extends 'base.html.twig' %}



{% block content %}

    {{ form_start(form, { 'attr': {'class': 'form-horizontal'} }) }}
    <div class="btn-toolbar" style="float:left">
        <div class="btn-group">
            <a class="btn btn-default btn-sm" href="{{ path('item_lista') }}">Volver</a>
        </div>
    </div>
    <br><br>
    <table width="100%" class="table table-condensed table-bordered tabla-datos ">
        <tr>
            <td  class="titulo">Id</td>
            <td class="td-in-well">{{ arItem.codigoItemPk }}</td>
        </tr>
        <tr>
            <td  class="titulo">Descripcion</td>
            <td class="td-in-well">{{ arItem.descripcion }}</td>
        </tr>
    </table>
    {{ form_end(form) }}

{% endblock %}




































