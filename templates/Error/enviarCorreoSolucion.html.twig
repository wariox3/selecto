<table>
    <thead style="background-color: #efefef;color: #202020;">Señor {{ arError.nombreUsuario }}, hemos corregido el error que se le presento el dia <span style="background-color: #2e6da4;color: white;padding: 5px;" >{{ arError.fecha | date('Y-m-d H:i') }}</span>

    </thead><br>
    <tbody>
    <tr>
        <td colspan="2" style="color: #ffffff; background-color: #2e6da4;padding: 8px;">
            <p>En nuestra plataforma de seguimiento detectamos que el dia {{ arError.fecha | date('Y-m-d H:i') }} el Sosial ERP arrojo un error, estamos
            comprometidos en mejorar nuestro servicio, hacer del cliente y toda su experiencia en la plataforma una prioridad, por esto, queremos informarle que
            el error fue atendido, analizado y solucionado</p>
        </td>
    </tr>
    <tr>
        <td><strong>Número: </strong></td>
        <td><span>{{ arError.codigoErrorPk }}</span></td>
    </tr>
    <tr>
        <td><strong>Fecha de Registro:</strong></td>
        <td><span>{{ arError.fecha | date('Y-m-d H:i') }}</span></td>
    </tr>
    <tr>
        <td><strong>Usuario:</strong></td>
        <td><span>{{ arError.nombreUsuario }} ({{ arError.usuario }})</span></td>
    </tr>
    <br>
    <tr>
        <td colspan="2" style="background-color: #2e6da4;color: #ffffff;text-align: center;">
            <h6>Por favor no responder este correo ya que es generado automaticamente.</h6>
        </td>
    </tr>
    </tbody>
</table>